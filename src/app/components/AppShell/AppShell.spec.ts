// Sample test file using Jest. See docs here: https://jestjs.io
// Test files can be named <name>.[spec | test].[ts | tsx] or located in a __tests__ directory for Jest to pick up
// const sum = (...a: number[]) =>
//         a.reduce((acc, val) => acc + val, 0);

// test('basic', () => {
//     expect(sum()).toBe(0);
// });

// test('basic again', () => {
//     expect(sum(1, 2)).toBe(3);
// });